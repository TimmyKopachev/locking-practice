spring:
  #Liquibase
  liquibase:
    change-log: ${LIQUIBASE_CHANGE_LOG:classpath:db-changelog/changelog-dev.xml}

  #JPA
  datasource:
    url: jdbc:h2:mem:account-storage-db-test;DB_CLOSE_DELAY=-1
    username: admin
    password:
